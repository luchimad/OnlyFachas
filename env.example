# OnlyFachas - Variables de Entorno de Emergencia
# Configura estas variables en Netlify para controlar la app en casos de emergencia

# Modo de Mantenimiento
# true = Activa el modo mantenimiento (bloquea toda la app)
# false = App funciona normalmente
VITE_MAINTENANCE_MODE=false

# Límite de Requests por Hora por IP
# Número máximo de análisis que puede hacer un usuario por hora
# Por defecto: 10
VITE_MAX_REQUESTS_PER_HOUR=10

# Delay entre Requests (en segundos)
# Tiempo de espera entre cada análisis para evitar sobrecarga
# Por defecto: 3 segundos
VITE_REQUEST_DELAY=3

# Variables adicionales (opcionales)
# VITE_AI_TIMEOUT=30000
# VITE_AI_RETRY_ATTEMPTS=2
# VITE_AI_FALLBACK_MODE=true
# VITE_MAX_IMAGE_SIZE=5242880
# VITE_DAILY_LIMIT_PER_USER=5
# VITE_WEEKLY_LIMIT_PER_USER=20
